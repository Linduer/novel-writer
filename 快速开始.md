# 快速开始指南

## 1. 安装依赖

```bash
pip install -r requirements.txt
```

## 2. 配置模型

复制配置文件并填入GLM API密钥：

```bash
copy config.example.yaml config.yaml
```

编辑 `config.yaml`，填入你的GLM API密钥。

## 3. 初始化项目

```bash
python main.py init --name "全民海上求生"
```

## 4. 导入现有材料

将你现有的材料复制到项目目录：

```bash
python import_existing.py
```

选择"1. 导入现有材料"，输入项目名称。

## 5. 开始写作

```bash
python main.py write --chapter 1
```

## 6. 审查章节

```bash
python main.py review --chapter 1
```

## 7. 查看状态

```bash
python main.py status
```

## 目录结构说明

```
data/全民海上求生/
├── outline/          # 大纲文件
│   └── 全书大纲.txt
├── characters/       # 角色档案
│   └── 角色档案.txt
├── world/           # 世界观设定
│   ├── 故事圣经.txt
│   └── 伏笔.txt
├── chapters/        # 章节内容
│   ├── drafts/      # 草稿
│   └── final/       # 最终版
└── memory/          # 记忆数据
    ├── facts/       # 事实表
    ├── summaries/   # 摘要
    └── vectors/     # 向量索引
```

## 工作流程

1. **准备阶段**：导入大纲、角色、世界观等设定材料
2. **写作阶段**：逐章写作，系统自动管理上下文
3. **审查阶段**：检查一致性，修复问题
4. **记忆更新**：系统自动提取事实，更新记忆
5. **继续写作**：基于更新后的记忆继续下一章

## 常见问题

### Q: 如何处理超长上下文？
A: 系统采用分层记忆和智能检索，自动管理上下文窗口。

### Q: 如何保证角色一致性？
A: 系统会追踪角色状态，在写作时自动加载相关设定。

### Q: 如何管理伏笔？
A: 系统会自动检测伏笔，并在适当时候提醒回收。

### Q: 可以导出小说吗？
A: 目前支持导出为TXT格式，后续会支持DOCX。

## 技术支持

如有问题，请查看项目文档或提交Issue。