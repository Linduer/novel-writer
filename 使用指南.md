# 长篇小说写作助手 - 使用指南

## 一、系统概述

这是一个基于GLM5.2/5.3的超长小说（100万字+）上下文管理系统，专门解决AI写作长篇小说时的上下文丢失问题。

### 核心优势
1. **分层记忆** - 全局、卷级、章级、片段多层记忆
2. **智能检索** - 自动加载相关上下文
3. **一致性检查** - 角色、时间线、地点等多维度检查
4. **GLM集成** - 专为GLM5.2/5.3优化

## 二、快速开始

### 1. 环境准备
```bash
# 进入项目目录
cd novel-writer

# 安装依赖
pip install -r requirements.txt
```

### 2. 配置模型
```bash
# 复制配置文件
copy config.example.yaml config.yaml

# 编辑config.yaml，填入你的GLM API密钥
# 在 https://open.bigmodel.cn/ 获取API密钥
```

### 3. 导入现有材料
```bash
# 运行导入脚本
python import_existing.py

# 选择"1. 导入现有材料"
# 输入项目名称：全民海上求生
```

### 4. 开始写作
```bash
# 写第1章
python main.py write --chapter 1

# 审查第1章
python main.py review --chapter 1

# 查看项目状态
python main.py status
```

## 三、详细使用流程

### 第一步：项目初始化
```bash
python main.py init --name "你的小说名称"
```

这会创建以下目录结构：
```
data/你的小说名称/
├── outline/          # 大纲文件
├── characters/       # 角色档案
├── world/           # 世界观设定
├── chapters/        # 章节内容
│   ├── drafts/      # 草稿
│   └── final/       # 最终版
└── memory/          # 记忆数据
    ├── facts/       # 事实表
    ├── summaries/   # 摘要
    └── vectors/     # 向量索引
```

### 第二步：导入设定材料

将你的小说材料放入对应目录：
- `outline/` - 放入大纲文件（如：全书大纲.txt）
- `characters/` - 放入角色档案（如：角色档案.txt）
- `world/` - 放入世界观设定（如：故事圣经.txt、伏笔.txt）

### 第三步：逐章写作

```bash
# 写第1章
python main.py write --chapter 1

# 系统会自动：
# 1. 构建写作上下文
# 2. 生成章节草稿
# 3. 进行一致性检查
# 4. 提取事实并更新记忆
```

### 第四步：审查与修改

```bash
# 审查章节
python main.py review --chapter 1

# 自动修复问题
python main.py review --chapter 1 --auto-fix
```

### 第五步：继续写作

```bash
# 写第2章（系统会自动加载前文上下文）
python main.py write --chapter 2

# 查看项目状态
python main.py status
```

## 四、核心功能详解

### 1. 分层记忆系统
- **全局记忆**：世界观、核心设定（永久存储）
- **卷级记忆**：每卷摘要、角色状态变化
- **章级记忆**：每章摘要、新事实提取
- **片段记忆**：相关章节检索（RAG）

### 2. 智能上下文装配
系统会根据当前章节需求，自动组装相关上下文：
- 当前卷的大纲和设定
- 相关角色的信息
- 前文的关键事实和摘要
- 历史章节的相关片段

### 3. 一致性保障
- **角色状态追踪**：位置、能力、关系变化
- **伏笔管理**：登记、推进、回收提醒
- **时间线管理**：确保时间逻辑合理
- **事实表自动更新**：每章写完自动提取新事实

### 4. GLM模型集成
系统使用三个不同的GLM角色：
- **写手**：负责创意写作（温度0.7）
- **档案员**：负责分析提取（温度0.3）
- **审查员**：负责质量检查（温度0.5）

## 五、配置说明

### config.yaml 主要配置项

```yaml
# 项目基本信息
project:
  name: "你的小说名称"
  chapters: 960  # 总章节数
  volumes: 10    # 总卷数

# GLM模型配置
llm:
  writer:
    model: "glm-5.2"  # 或 glm-5.3
    temperature: 0.7
    api_key: "your_api_key_here"
  
  archivist:
    model: "glm-5.2"
    temperature: 0.3
    api_key: "your_api_key_here"
  
  reviewer:
    model: "glm-5.2"
    temperature: 0.5
    api_key: "your_api_key_here"

# 上下文配置
context:
  budget:
    system_rules: 5      # 系统规则占比5%
    character_cards: 15  # 角色卡片占比15%
    summaries: 20        # 历史摘要占比20%
    current_draft: 30    # 当前草稿占比30%
```

## 六、常见问题

### Q: 如何处理超长上下文？
A: 系统采用分层记忆和智能检索，自动管理上下文窗口。每章写作时，只加载最相关的上下文信息。

### Q: 如何保证角色一致性？
A: 系统会追踪角色状态，在写作时自动加载相关设定。每章写完后，会自动提取新事实并更新记忆。

### Q: 如何管理伏笔？
A: 系统会自动检测伏笔，并在适当时候提醒回收。你可以在`world/foreshadowing.txt`中管理伏笔列表。

### Q: 可以导出小说吗？
A: 目前支持导出为TXT格式，后续会支持DOCX。你可以直接从`chapters/final/`目录复制文件。

### Q: 如何修改写作风格？
A: 编辑`prompts/writer.md`文件，修改写手提示词即可。

## 七、进阶使用

### 1. 自定义提示词
编辑`prompts/`目录下的提示词模板，可以调整AI的写作风格和检查规则。

### 2. 批量写作
```bash
# 连续写多章（示例：写第1-10章）
for i in {1..10}; do
  python main.py write --chapter $i
done
```

### 3. 查看记忆数据
```bash
# 查看事实表
type data\全民海上求生\memory\facts\facts.jsonl

# 查看摘要
dir data\全民海上求生\memory\summaries
```

### 4. 备份项目
```bash
# 系统会自动备份，也可以手动复制
xcopy data\全民海上求生 backups\全民海上求生 /E /I
```

## 八、技术架构

### 模块说明
- `core/storage.py` - 文件存储管理
- `core/memory.py` - 记忆系统（事实表、摘要、向量）
- `core/context.py` - 上下文引擎（智能检索、Token预算）
- `core/llm.py` - GLM API封装
- `core/checker.py` - 一致性检查

### 数据流
1. 写作前：加载项目数据 → 构建上下文 → 装配给LLM
2. 写作中：生成草稿 → 一致性检查
3. 写作后：提取事实 → 更新记忆 → 保存章节

## 九、注意事项

1. **API密钥安全**：不要将config.yaml提交到公开仓库
2. **备份重要**：定期备份项目数据
3. **逐步优化**：先跑起来，再根据需要调整配置
4. **人工审查**：AI生成的内容仍需人工审查

## 十、获取帮助

- 查看`快速开始.md`了解基本操作
- 查看`README.md`了解项目概述
- 查看源代码了解实现细节

---

**祝你创作顺利！**